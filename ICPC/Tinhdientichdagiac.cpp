#include <bits/stdc++.h>
using namespace std;
#ifdef MaiLinh
#include <debug.h>
#else
#define debug(...) 12
#endif
#define ll long long
#define mod int(1e9 + 7)
#define nmax int(1e6 + 7)
/* 
Tính diện tích của một đa giác bất kì (cho trước thứ tự các đỉnh) ta dùng công thức S = 1/2 | tổng(i=1->n)(x_i * y_i+1 - x_i+1 * y_1)| với x_n+1, y_n+1 = x1, y1
*/
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n;
    cin >> n;

    
    return 0;
}
